# Today

A small, dependency-free page that displays the visitor's current local date, time, and time zone. Visitors can add and remove personal tags; tags are saved in the browser with `localStorage`.

## Run locally

```bash
npm start
```

Then open <http://localhost:8000>.

## Test

```bash
npm test
```
